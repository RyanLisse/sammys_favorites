# RJC-261 provider and compliance feasibility

Status: **Human Review / at risk**. G0 remains blocked. This is a dated official-document observation and safe-fallback decision, not provider onboarding evidence or legal advice.

Detailed evidence-gap artifacts:

- [Provider feasibility matrix](provider-evidence-matrix.md)
- [Compliance evidence matrix](compliance-evidence-matrix.md)

## Official-source observations (retrieved 2026-08-01)

### Stripe

- [Testing](https://docs.stripe.com/testing) requires account sandbox access and a sandbox secret key. The intended integration must use a restricted key limited to its PaymentIntent resources. Test fixtures do not prove the account can create a PaymentIntent.
- [Webhook signature verification](https://docs.stripe.com/webhooks/signature) requires the unmodified raw request body, `Stripe-Signature` header, and endpoint-specific signing secret. A registered endpoint, selected event types, real signature, duplicate delivery, and stale delivery must be evidenced.
- Decision: checkout and payment collection stay disabled until a sandbox payment and signed-webhook/idempotency receipt exist.

### Meta WhatsApp Cloud API

> **Superseded 2026-08-03 by [ADR 0003](../decisions/0003-telegram-channel-pivot.md).** Retained unedited as observed on 2026-08-01. See the addendum at the end of this document.

- [Getting started](https://developers.facebook.com/documentation/business-messaging/whatsapp/get-started) depends on a Meta business portfolio, WABA, registered business phone number, suitable approved template, and observed grants for `whatsapp_business_messaging` and `whatsapp_business_management` (`business_management` only where the chosen business-management endpoint requires it).
- [Webhooks](https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/overview) require an HTTPS callback, verification token, subscribed WABA, and app-secret-based `X-Hub-Signature-256` validation. The customer-service window, consent, opt-out, retry, and delivery reconciliation need live configuration evidence.
- Decision: Atelier remains the authenticated operating surface and customer communication is manual. No automated WhatsApp delivery is enabled.

### Chat SDK

- [Chat SDK documentation](https://chat-sdk.dev/docs) is an implementation framework reference. Its official WhatsApp adapter is currently Beta; it does not confer Meta permissions, prove channel ownership, or prove provider delivery.
- A future adapter needs pinned package/adapter versions, an explicit Graph API version supported by both Meta and the adapter, provider credentials outside Git, least-privilege provider permissions, and inbound/outbound, duplicate, replay, attachment-size, and delivery-status conformance.
- Decision: the channel adapter remains disabled and proposal flows use local fixtures only.

### AliExpress Open Platform

- The [Open Platform portal](https://open.aliexpress.com) is authoritative for the exact API list granted to an approved developer/application. Required prerequisites are application approval, an app key/secret and authorized token outside Git, and applicable program/supplier eligibility.
- The eventual grant must explicitly cover `aliexpress.ds.product.get`, `aliexpress.ds.freight.query`, `aliexpress.ds.order.create`, `aliexpress.trade.ds.order.get`, and `aliexpress.ds.order.tracking.get`. Portal reachability proves none of approval, grants, rate limits, ordering eligibility, or a test facility.
- Decision: scraping is prohibited; Samantha performs sourcing, ordering, and tracking manually; `SupplierOrderWritePort` stays disabled.

## Compliance ownership and gate decision

Ryan Lisse coordinates specialist review for GPSR, exact-SKU materials/hallmark claims, consumer rights, VAT/IOSS/importer-of-record, packaging/EPR, and privacy. That accountability is not specialist approval; sign-off remains externally pending.

The compliance matrix records no green row. Two time-sensitive findings require specialist confirmation against the actual operating model: current Dutch guidance records that the low-value e-commerce import-duty exemption ended on 1 July 2026, and the EU Packaging and Packaging Waste Regulation applies from 12 August 2026.

`p0-gate-report.json` is the CI-readable source. G0 remains `blocked_external`; G1 remains `in_progress` until RJC-256–260 pass from merged `origin/main`. P1 and P2 may not start while either gate is not green.

## 2026-08-03 addendum: messaging channel changed to Telegram

[ADR 0003](../decisions/0003-telegram-channel-pivot.md) replaces Meta WhatsApp Cloud with the Telegram Bot API as the automated messaging channel. The full dated findings are in the [provider matrix addendum](provider-evidence-matrix.md#2026-08-03-addendum-telegram-replaces-whatsapp).

Why: the WhatsApp lane is gated on assets and approvals that only Meta issues — business portfolio, WABA, owned sender number, app review, permission grants, template approval. None has a date Sammy's Favorites controls. Telegram's credential chain is a single `@BotFather` bot token, so the lane's evidence can be produced rather than waited for.

What this changes: the Chat SDK adapter target becomes `@chat-adapter/telegram@4.36.0` against the same `chat@4.36.0`; webhook authenticity becomes a constant-time comparison of the `X-Telegram-Bot-Api-Secret-Token` header against the `secret_token` registered through `setWebhook`; duplicate suppression keys on `update_id`.

What this does not change: `G0` is still not green, automated messaging is still disabled, and the fallback is unchanged — the authenticated Atelier surface plus manual customer communication. `p1AndP2MayStart` stays false.

The cost is recorded in the ADR rather than buried here: WhatsApp is near-universal in the Netherlands and Telegram is not, and the assumption that customers will accept Telegram is untested.

Two other dated corrections belong to the same review: `https://chat-sdk.dev/adapters` now returns HTTP 404 where 2026-08-01 recorded HTTP 200, and Stripe's official MCP documentation describes a tool catalog broader than the curated variant RJC-300 connected to — a documentation-level finding, not a provider observation, and not a cleared blocker.
