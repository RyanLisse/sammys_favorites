# Sammy's Favorites

Follow the repository-wide instructions in `../AGENTS.md`.

Keep changes focused, preserve accessibility and type safety, and verify the smallest relevant surface before reporting completion. Use `pnpm dlx ultracite fix` for formatting and safe lint fixes, then `pnpm dlx ultracite check` for validation.

Never read, print, or commit secrets. Treat `.env*`, private keys, and credentials as sensitive.
